<div class="postsContainer">
    {{#each posts}}
        {{> post}}
    {{/each}}
</div>

{{!-- TODO: Fix

{{#if logged_in}}
    <div id="chat-container"></div>
        {{> chat}}
    </div>
{{/if}} --}}

{{> postbox}}